apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: agent-platform
spec:
  template:
    spec:
      containers:
        - name: frontend
          command: ['sh', '-c', 'npm run build && npm start']
          env:
            - name: NODE_ENV
              value: production
