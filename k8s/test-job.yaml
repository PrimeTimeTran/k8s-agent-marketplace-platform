apiVersion: batch/v1
kind: Job
metadata:
  name: test-job
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 30
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: runner
          image: test-job:latest
          imagePullPolicy: IfNotPresent
